#!/bin/sh

script_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
share_dir=$(CDPATH='' cd -- "$script_dir/../share" && pwd)

export PYTHONPATH="$share_dir${PYTHONPATH:+:$PYTHONPATH}"

exec python3 -c 'from gitrepo.build_iso.cli import main; raise SystemExit(main())' "$@"
